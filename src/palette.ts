import { PALETTE, RGB } from "./types";

export function set_palette(pal: PALETTE | RGB) {
  void pal;
}

export const desktop_palette: PALETTE = [{ r: 0, g: 0, b: 0 }];

export const palette_color: number[] = [0, 1, 2, 3, 4, 5, 6, 7];
